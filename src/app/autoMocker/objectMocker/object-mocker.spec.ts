import {before} from "selenium-webdriver/testing";
import {ObjectMocker} from "./object-mocker";
describe('getting Properties properly', () => {
  /*
  before(() => {
    this.obj = {
      field: 'field',
      func: () => {
        return true;
      },
    };
  });
*/
  /*
  it('should include functions', () => {
    let propertyArray = ObjectMocker.getAllPropertiesOf(this.obj);
    expect(propertyArray.indexOf('func') <= 0);
  });

  it('should include fields', () => {
    let propertyArray = ObjectMocker.getAllPropertiesOf(this.obj);
    expect(propertyArray.indexOf('field') <= 0);
  });
*/

});

